import { Metadata } from 'next';
import { pageMetadata } from './config';

// Voice Agent page
export const voiceMetadata: Metadata = pageMetadata.voice;

// Lead Generation page
export const leadGenerationMetadata: Metadata = pageMetadata.leadGeneration;

// Content Studio page
export const contentStudioMetadata: Metadata = pageMetadata.contentStudio;

// Resume Parser page
export const resumeParserMetadata: Metadata = pageMetadata.resumeParser;

// Interview Center page
export const interviewCenterMetadata: Metadata = pageMetadata.interviewCenter;

// Recruiter Intel page
export const recruiterIntelMetadata: Metadata = pageMetadata.recruiterIntel;

// Tasks page
export const tasksMetadata: Metadata = pageMetadata.tasks;

// Outreach page
export const outreachMetadata: Metadata = pageMetadata.outreach;

// Orchestrator page
export const orchestratorMetadata: Metadata = pageMetadata.orchestrator;

// Firecrawl page
export const firecrawlMetadata: Metadata = pageMetadata.firecrawl;

// LinkedIn page
export const linkedinMetadata: Metadata = pageMetadata.linkedin;

// Twilio page
export const twilioMetadata: Metadata = pageMetadata.twilio;

// SharePoint page
export const sharepointMetadata: Metadata = pageMetadata.sharepoint;

// Settings page
export const settingsMetadata: Metadata = pageMetadata.settings;